doctype html
html(lang="en")
  head
    title= "User Index"
  body
    h1 Pug 🐶
    div
      each user in users
        div
          img(src=`${user.username}_sm.jpg`)
          a(href=`/users/${user.username}`)= user.name.full